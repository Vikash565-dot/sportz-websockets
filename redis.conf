# Cache-only Redis configuration for this application.
# Size this to the memory available in your deployment environment.
maxmemory 256mb
maxmemory-policy allkeys-lfu

# Keep counters decaying so old popularity does not permanently dominate eviction.
lfu-decay-time 1
lfu-log-factor 10
